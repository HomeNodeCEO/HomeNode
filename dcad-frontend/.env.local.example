# Point the Vite dev server proxy to the Node API during development
VITE_PROXY_TARGET=http://127.0.0.1:4000

# Alternatively, set an absolute API base and skip the proxy
# VITE_API_URL=http://127.0.0.1:4000
